node_modules/
dist/
coverage/
.cork-ai/
# Working plan — local only, never committed
PLAN*.md
*.log
.env
.DS_Store
Thumbs.db
release/

# A/B campaign output: paid runs, repo copies, per-run homes. Never committed;
# the curated, anonymised subset lives in docs/evidence/raw/.
ab-repo-results/
